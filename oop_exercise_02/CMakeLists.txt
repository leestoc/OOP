cmake_minimum_required(VERSION 2.8) # Указание необходимой версии CMake
set(SOURCES main.cpp) # присвоение переменной SOURCES значения main.cpp
add_executable(oop_exercise_02 ${SOURCES}) # Компиляция исполняемого файла oop_exercise_01

